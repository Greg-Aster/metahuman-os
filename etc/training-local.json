{
  "base_model": "unsloth/Qwen3.5-9B",
  "max_seq_length": 2048,
  "lora_rank": 8,
  "lora_alpha": 16,
  "lora_dropout": 0,
  "num_train_epochs": 2,
  "learning_rate": 0.00012,
  "per_device_train_batch_size": 1,
  "gradient_accumulation_steps": 16,
  "optimizer": "paged_adamw_8bit",
  "load_in_4bit": false,
  "load_in_16bit": true,
  "dtype": "bfloat16",
  "use_gradient_checkpointing": true,
  "fp16": false,
  "bf16": true,
  "logging_steps": 10,
  "save_strategy": "epoch",
  "save_total_limit": 2,
  "chat_template": "chatml",
  "system_prompt": "You are MetaHuman Greg, a helpful assistant.",
  "comment": "Maintained Qwen 3.5 text-only 16-bit LoRA configuration",
  "notes": {
    "model_size": "9B parameters using 16-bit LoRA training",
    "vram_usage": "Approximately 22GB for bf16 LoRA; use a 24GB-class GPU or larger",
    "training_speed": "Depends on accelerator, sequence length, and dataset size",
    "effective_batch_size": "per_device_train_batch_size (1) × grad_accum (16) = 16",
    "chat_template": "chatml - Qwen format using <|im_start|> / <|im_end|> tokens",
    "license": "Apache 2.0",
    "use_case": "Maintained Qwen 3.5 text-only LoRA training",
    "supported_templates": "chatml | auto (auto-detect from model)",
    "architecture": "Qwen 3.5 language model with vision encoder; text-only language layers are trained"
  }
}
