<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External files directory for APK downloads -->
    <external-files-path
        name="downloads"
        path="Download/" />

    <!-- Cache directory -->
    <cache-path
        name="cache"
        path="/" />

    <!-- External cache -->
    <external-cache-path
        name="external_cache"
        path="/" />
</paths>
