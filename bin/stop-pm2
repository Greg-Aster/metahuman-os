#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

if command -v pm2 >/dev/null 2>&1 && pm2 describe metahuman-os >/dev/null 2>&1; then
  pm2 delete metahuman-os
fi

"$REPO_ROOT/stop.sh"
