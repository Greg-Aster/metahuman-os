#!/usr/bin/env bash
# Start shared voice inference servers configured for system boot.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec "$REPO_ROOT/bin/mh" voice-server start --all --boot
