/** Brain-owned entrypoint for the React Native backend bundle. */

export { startLocalModelService } from '@metahuman/core/mobile-handlers'
export { initializeMobileAgents, stopMobileAgents } from './mobile-agents.js'
